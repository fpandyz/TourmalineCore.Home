export enum Breakpoint {
  MOBILE = 375,
  TABLET = 768,
  TABLET_XL = 1024,
  DESKTOP = 1366,
  DESKTOP_XL = 1920,
}

export enum BreakpointName {
  MOBILE = `mobile`,
  TABLET = `tablet`,
  TABLET_XL = `tablet-xl`,
  DESKTOP = `desktop`,
  DESKTOP_XL = `desktop-xl`,
}
